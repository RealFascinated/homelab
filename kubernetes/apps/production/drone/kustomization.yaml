---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: drone-ci
resources:
  - namespace.yaml
  - sealed-secrets.yaml

  # Server
  - pvc.yaml
  - server-deployment.yaml
  - service.yaml
  - ingress.yaml

  # Runner
  - runner-deployment.yaml
  - rbac.yaml
